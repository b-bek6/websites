// Type conversion = change the datatype of a value to another (string, number, booleans)

// let age = window.prompt("Enter your age: "); // This will always take string.
// age = Number(age); // Converts string into number.
// age += 1; // age = age + 1;

// alert("Happy birthday!!! You are "+age+" years old");

let x;
let y;
let z;

x = Number("3.14") // string into number
y = String(3.14) // number into string
z = Boolean("3.14") // string into number

console.log(x, typeof x);
console.log(y, typeof y);
console.log(z, typeof z);